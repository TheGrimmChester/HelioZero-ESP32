.DS_Store
.cursor/
AGENTS.md
# Local dev trees (mock Docker, lab notes) — see _local/README.example when present
_local/
# PlatformIO may emit a .cpp next to the sketch; source of truth is HelioZero.ino
firmware/*.ino.cpp
# Local API captures (use firmware/test/golden/ for committed fixtures)
data/
.venv/
.venv-pio/
.pio/
# PlatformIO compiledb — machine-local absolute paths (clangd); regenerate: pio run -t compiledb
compile_commands.json
web/node_modules/
web/dist/
web/coverage/
firmware/coverage/
*.profraw
web/.vite/
web/.env.local
platformio.local.ini
# Field-help markdown generated by web/scripts/generate-field-help-docs.ts (CI + pretest)
web/.field-help-docs/
# Generated on npm run build (sync-brand-assets.mjs, generate-pwa-icons.mjs)
web/public/

# Local upstream snapshot trees — never commit
upstream-snapshot/
vendor-local/
