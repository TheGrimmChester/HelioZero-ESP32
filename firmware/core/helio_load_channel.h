#pragma once

/*
 * helio_load_channel.h — Load channel config (schedules, GPIO/HTTP, triac).
 * Implementation: Actions.h, Actions.cpp (config/wire), helio_load_runtime.cpp (GPIO/HTTP).
 */
#include "Actions.h"

using HelioLoadChannel = Action;
